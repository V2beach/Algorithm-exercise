#pragma warning(disable: 4996)
#include <cstdio>
#include <algorithm>

using namespace std;

int main(int argc, char *argv[]) {
	int Cmax, D, Davg, N;
	scanf("%d %d %d %d", &Cmax, &D, &Davg, &N);
	for (int i = 0; i < )
	system("pause");
	return 0;
}